# Source provenance

This directory preserves the supplied final Visual-to-Code reasoning prompt as source material.

The active Codex instruction system is the repository root documentation plus `.agents/skills/`.

Do not treat archived/source prompt wording as a higher-priority active rule when it conflicts with the current root project rules.
